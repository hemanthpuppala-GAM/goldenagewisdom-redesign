Upload BOTH files into public_html/goldenagewisdom.org/ — overwrite when asked.

  signup.php      form handler (rate limit raised from 5 to 20 per hour)
  volunteer.html  volunteer page (now shows the server's real message
                  instead of the generic "we could not reach the server")

Afterwards hard-refresh the page: Ctrl+Shift+R  (Cmd+Shift+R on Mac).
